{
  "name": "publish-requests-worker",
  "version": "1.0.0",
  "description": "Publish Request Workflow Worker - Cloudflare Worker",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:prod": "wrangler deploy --env prod",
    "tail": "wrangler tail"
  },
  "dependencies": {
    "jose": "^5.10.0"
  },
  "devDependencies": {
    "wrangler": "^4.62.0"
  }
}
